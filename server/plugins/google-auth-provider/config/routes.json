{
  "routes": [
    {
      "method": "GET",
      "path": "/",
      "handler": "google.index",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/initGoogleLogin",
      "handler": "google.initGoogleLogin",
      "config": {
        "policies": []
      }
    }

  ]
}
