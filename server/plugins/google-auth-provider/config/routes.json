{
  "routes": [
    {
      "method": "GET",
      "path": "/",
      "handler": "google.index",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/credentials",
      "handler": "google.credentials",
      "config": {
        "policies": []
      }
    }

  ]
}
